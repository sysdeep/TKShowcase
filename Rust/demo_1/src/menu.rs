// use tk::cmd::*;
// use tk::*;

// pub fn make_menu<Inst: TkInstance>(root: TkRoot<Inst>) {
//     let main_menu = root.add_menu(());
//     root.configure(-menu(main_menu));

//     let win = root.add_toplevel(())?;
//     let menubar = win.add_menu(())?;
//     win.configure(-menu(menubar))?;
// }
